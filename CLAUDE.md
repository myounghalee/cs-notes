# CS 노트 작성 가이드

이 폴더(`Note/CS`)는 사용자가 컴퓨터 과학 지식을 정리해나가는 Obsidian vault입니다. 이 폴더 안에서 노트를 새로 쓰거나 수정할 때는 아래 규칙을 따르세요.

## 목적

- 1차 목적: **면접 대비용 CS 지식 정리**. 초급자가 읽어도 이해할 수 있을 만큼 쉽게 설명하되, 실제 기술 면접에서 나올 법한 깊이는 유지한다.

## 폴더 구조

- **주제별 폴더**로 분류하고, **폴더명은 영어로 통일**한다. 예: `Java/`, `Spring/`, `Database/`, `AI/`, `Infrastructure/`, `Fundamentals/`, `Software Engineering/`, `Algorithms/`, `Data Structures/`.
  - 폴더 안 노트 파일명과 본문 내용은 기존처럼 한글로 작성한다 — 영어 통일은 폴더명에만 적용(2026-08-30 확정).
  - 네트워크(통신/프로토콜)와 인프라(미들웨어/분산 시스템 컴포넌트)처럼 경계가 모호한 주제는 억지로 나누지 않고 하나의 폴더(`Infrastructure/`)로 합친다.
  - 특정 기술 스택에 매이지 않는 범용 CS 이론(운영체제, Git, 보안 이론 등)은 `Fundamentals/` 폴더 하나에 모은다 — 매번 주제마다 얇은 폴더를 새로 만들지 않기 위함(2026-09-01 확정, 기존 `Operating System/`·`Git/` 폴더를 통합).
- 새 주제 폴더를 만들어야 할 때는 만들기 전에 사용자에게 폴더명을 확인한다.
- 한 폴더 안에서도 특정 하위 주제가 방대해지면 **별도 파일로 분리**한다. 상위 문서에는 간단히 언급 + `[[링크]]`만 남기고, 상세 내용은 분리된 문서에 담는다.
  - 예: `Java/JVM.md`는 GC를 간단히만 언급하고, `Java/GC.md`에 상세 내용을 담음.

## 글쓰기 스타일 — "블로그처럼, 초급자도 이해하게"

딱딱한 사전식 정의 나열이 아니라, **아래 순서**로 개념을 풀어서 설명한다.

1. **비유로 먼저 감을 잡게 한다.** 일상적인 사물/상황에 빗대어 "이게 왜 필요한지, 어떤 느낌인지"부터 전달.
2. **실제로 동작하는 Java(또는 해당 언어) 코드 예시**를 붙인다. 추상적인 설명에 그치지 않고 눈으로 확인할 수 있게.
3. **구조나 흐름은 다이어그램으로 시각화**한다. Obsidian은 별도 플러그인 없이 \`\`\`mermaid\`\`\` 코드블록을 네이티브 렌더링하므로, `graph`/`flowchart`/`classDiagram` 등을 적극 활용한다. (계층 구조, 처리 흐름, 메모리 구조, 비교 관계 등)
4. **비교가 필요한 내용은 표로 정리**한다 (예: A vs B 비교).

## 문서 공통 구성 (템플릿)

```markdown
#CS #하위주제 #면접

관련: [[다른노트1]] · [[다른노트2]]

## 목차
- [[#(개념 1) — 비유로 시작하는 소제목]]
- [[#(개념 2) ...]]
- [[#한 장 요약]]
- [[#Q&A로 복습하기]]

---

## (개념 1) — 비유로 시작하는 소제목

비유 설명 → 코드 예시 → (필요시) 다이어그램 → 왜 중요한지/실무에서 어떻게 쓰이는지

## (개념 2) ...

...

---

## 한 장 요약

| 질문 | 답 |
|---|---|
| ... | ... |

## Q&A로 복습하기

### Q. (본문 내용을 확인하는 질문)?
A. (본문에서 이미 다룬 내용을 바탕으로 한 답변, 1~3문장)

### Q. ...?
A. ...
```

- **최상단 메타**: `#CS #하위주제 #면접` 형태의 해시태그 + `관련: [[노트]]`로 같은 폴더 내 다른 노트와 상호 링크.
- **최하단**: "한 장 요약" 표 + "Q&A로 복습하기" 섹션은 거의 모든 문서에 유지한다.
  - "Q&A로 복습하기"는 **압박 면접용 꼬리질문이 아니라, 방금 읽은 본문 내용을 스스로 확인하는 복습용 질문**이다. 질문만 던지지 말고 반드시 답까지 함께 적는다. 본문에서 다루지 않은 지나치게 지엽적이거나 어려운 질문은 넣지 않는다. 보통 4~6개 정도.

## 작업 방식

- 콘텐츠는 Claude가 CS 지식을 바탕으로 직접 작성해서 채워 넣는다. 사용자는 이후 검수/보완한다.
- 기존 노트와 겹치는 내용이 생기면, 새로 쓰기보다 기존 파일을 보완하거나 링크로 연결하는 쪽을 우선 고려한다.
- 이 가이드 자체가 바뀌면 (사용자가 스타일/구조에 대한 새 지침을 주면) 이 파일을 함께 업데이트한다.
- **새 노트를 추가하거나 삭제/이동하면 반드시 `목차.md`도 같이 업데이트한다.** (전체 현황은 여기 CLAUDE.md가 아니라 `목차.md`에 유지 — 중복 관리 방지)

## 전체 목차

폴더 현황과 각 노트 요약은 **[[목차]]** 참고.
